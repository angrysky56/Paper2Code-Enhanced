---
phase: 01-sandboxed-execution
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: [codes/4_debugging.py]
autonomous: true
requirements: [EXEC-03]
user_setup: []

must_haves:
  truths:
    - "4_debugging.py executes code using get_executor() rather than raw local subprocess.run calls"
    - "Sandbox execution returns structured ExecutorResult and matches existing stdout/stderr formatting"
  artifacts:
    - path: "codes/4_debugging.py"
      provides: "Secure debugging loop integrated with pluggable sandboxed executors"
      contains: "get_executor"
  key_links:
    - from: "codes/4_debugging.py"
      to: "codes/executor.py"
      via: "import get_executor"
      pattern: "from codes\\.executor import.*get_executor"
---

<objective>
Wire the sandboxed executor into 4_debugging.py replacing raw shell executions.

Purpose: Restrict all dynamic generated code execution to the sandboxed runtime, completely mitigating unsafe code execution concerns during self-debugging.
Output: Integrated codes/4_debugging.py using get_executor() factory.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sandboxed-execution/01-01-SUMMARY.md
@.planning/phases/01-sandboxed-execution/01-02-SUMMARY.md
@codes/4_debugging.py
@codes/executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate and replace raw subprocess execution in codes/4_debugging.py</name>
  <files>codes/4_debugging.py</files>
  <read_first>codes/4_debugging.py</read_first>
  <action>Import `get_executor` from `codes.executor` at the top of `codes/4_debugging.py`. Locate all occurrences of raw `subprocess.run` or `subprocess.Popen` that execute the generated code (e.g. running the python script to check compile errors). Replace them entirely with a call to `get_executor().run()`. Pass the appropriate working directory, environment variables, timeout parameters, and command string. Map the return properties of the ExecutorResult (exit_code, stdout, stderr, success) back into the existing error detection and patching flow.</action>
  <verify>Ensure `codes/4_debugging.py` executes generated code via the executor client.</verify>
  <acceptance_criteria>
    - codes/4_debugging.py contains NO raw subprocess execution of the compiled file.
    - get_executor() is used to run the compiled file and returns expected structures.
  </acceptance_criteria>
  <done>Raw local subprocess execution replaced with get_executor() sandbox runtime.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python codes/4_debugging.py --help` (or standard trial run) to ensure no syntax/runtime import regressions.
</verification>

<success_criteria>
- All tasks completed
- Generated PyTorch compilation checks run safely under the configured Sandboxed Executor.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sandboxed-execution/01-03-SUMMARY.md`
</output>
