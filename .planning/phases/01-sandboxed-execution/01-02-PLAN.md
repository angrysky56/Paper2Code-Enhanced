---
phase: 01-sandboxed-execution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [codes/executor.py]
autonomous: true
requirements: [EXEC-02]
user_setup: []

must_haves:
  truths:
    - "DockerExecutor executes commands inside a containerized sandbox using the docker SDK"
    - "DockerExecutor supports volume mounts, CPU caps, memory caps, and optional GPU runtime settings"
  artifacts:
    - path: "codes/executor.py"
      provides: "DockerExecutor implementation using docker Python SDK"
      contains: "import docker"
  key_links: []
---

<objective>
Implement PyTorch/CUDA-capable containerized runtime in DockerExecutor.

Purpose: Provide full virtualization sandbox for running generated code safely, preventing local system contamination while maintaining GPU speed.
Output: Production-ready DockerExecutor in codes/executor.py.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement container lifecycle and execution in DockerExecutor</name>
  <files>codes/executor.py</files>
  <read_first>codes/executor.py</read_first>
  <action>Implement DockerExecutor.run(). Use the `docker` python SDK client to pull/build the image if needed, run a container with appropriate mounts (workspace volume), configure CPU and memory limits (e.g. `mem_limit` and `nano_cpus`), set the working directory, execute the requested command, extract exit status and stdout/stderr logs, and ensure containers are strictly stopped and cleaned up (using try-finally) to prevent ghost containers.</action>
  <verify>Check that docker SDK is correctly imported and run method is fully fleshed out.</verify>
  <acceptance_criteria>
    - codes/executor.py has active docker client connection and full lifecycle management.
    - docker package is in pyproject.toml / requirements.txt.
  </acceptance_criteria>
  <done>Docker execution lifecycle implemented with mounts and boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Support GPU passthrough and NVIDIA runtime conditionally</name>
  <files>codes/executor.py</files>
  <read_first>codes/executor.py</read_first>
  <action>Add logic in DockerExecutor to enable NVIDIA GPU support when DOCKER_ENABLE_GPU env var is true. Specifically, add `device_requests=[docker.types.DeviceRequest(count=-1, capabilities=[["gpu"]])]` to the `containers.run` call, and handle fallback graceful degradation if the NVIDIA container toolkit is not available on the host system.</action>
  <verify>Verify conditional GPU requested in docker container launch arguments.</verify>
  <acceptance_criteria>
    - codes/executor.py checks DOCKER_ENABLE_GPU and applies DeviceRequest capabilities properly.
  </acceptance_criteria>
  <done>Conditional NVIDIA CUDA runtime support integrated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `codes/executor.py` syntax is correct.
- [ ] Run containerized execution test via `DockerExecutor` using a standard python image (e.g. `python:3.11-slim`) and ensure output matches.
</verification>

<success_criteria>
- All tasks completed
- DockerExecutor runs commands in isolated Docker containers safely and returns outputs cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sandboxed-execution/01-02-SUMMARY.md`
</output>
