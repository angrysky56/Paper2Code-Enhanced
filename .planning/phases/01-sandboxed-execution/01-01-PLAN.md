---
phase: 01-sandboxed-execution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [codes/executor.py]
autonomous: true
requirements: [EXEC-01]
user_setup: []

must_haves:
  truths:
    - "SubprocessExecutor enforces memory limits on Linux using resource setrlimit"
    - "Execution fails with ResourceLimitError or similar code when exceeding memory limits"
  artifacts:
    - path: "codes/executor.py"
      provides: "Resource-restricted SubprocessExecutor implementation"
      contains: "resource.setrlimit"
  key_links: []
---

<objective>
Implement memory boundaries in SubprocessExecutor using Linux resource setrlimits.

Purpose: Restrict memory usage of subprocess executions to prevent out-of-memory crashes on the host system.
Output: Production-ready SubprocessExecutor inside codes/executor.py with memory limit support.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add setrlimit call in preexec_fn of SubprocessExecutor</name>
  <files>codes/executor.py</files>
  <read_first>codes/executor.py</read_first>
  <action>Modify the preexec_fn hook in SubprocessExecutor. It must check if self.memory_limit_mb is greater than 0, and if so, call resource.setrlimit(resource.RLIMIT_AS, (self.memory_limit_mb * 1024 * 1024, self.memory_limit_mb * 1024 * 1024)). Handle any imports of the resource module safely (as it is Linux-only). Ensure any exceptions raised in the preexec_fn are captured and handled gracefully.</action>
  <verify>Run the unit tests or a test script verifying that SubprocessExecutor sets limits.</verify>
  <acceptance_criteria>
    - codes/executor.py contains "resource.setrlimit" inside the preexec_fn logic.
    - SubprocessExecutor can be instantiated with memory_limit_mb and run a simple command.
  </acceptance_criteria>
  <done>Memory limit configured in subprocess environment creation.</done>
</task>

<task type="auto">
  <name>Task 2: Handle memory out-of-memory terminations in ExecutorResult</name>
  <files>codes/executor.py</files>
  <read_first>codes/executor.py</read_first>
  <action>Ensure that when the subprocess gets terminated by SIGKILL, SIGSEGV, or returns a non-zero exit code due to memory limits, the ExecutorResult correctly captures the exit code, stderr, and indicates a resource limit error. Set result.success = False, and add a descriptive error in stdout/stderr.</action>
  <verify>Verify subprocess execution returns success=False and indicating memory limit error when constrained.</verify>
  <acceptance_criteria>
    - Exit code and error messages reflect resource exhaustion.
  </acceptance_criteria>
  <done>Memory limits correctly trigger execution failures with descriptive logs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `codes/executor.py` compiles without syntax errors.
- [ ] Run a test command through `SubprocessExecutor` with a small memory limit (e.g. 50MB) running a Python script that allocates 100MB of memory and verify it is terminated or fails.
</verification>

<success_criteria>
- All tasks completed
- SubprocessExecutor successfully enforces memory limits.
</success_criteria>

<output>
After completion, create `.planning/phases/01-sandboxed-execution/01-01-SUMMARY.md`
</output>
