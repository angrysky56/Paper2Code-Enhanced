---
phase: 03-structured-cli-interoperability
plan: 03-01
type: execute
wave: 1
depends_on: []
files_modified: [codes/pipeline.py]
autonomous: true
requirements: [CLI-01, CLI-02]
user_setup: []

must_haves:
  truths:
    - "pipeline.py parses --output-format and redirects all logs to stderr when output-format is json"
    - "pipeline.py executes stages sequentially via subprocess spawning, matching the selected stages"
    - "pipeline.py output to stdout under '--output-format json' is strictly a valid JSON payload"
  artifacts:
    - path: "codes/pipeline.py"
      provides: "Wired and tested structured CLI entrypoint"
    - path: "tests/test_cli_interop.py"
      provides: "Automated test suite verifying CLI arg parsing and stream separation"
---

<objective>
Standardize the CLI interface in `codes/pipeline.py` to support all necessary flags, separate `stdout`/`stderr` streams, execute stage scripts as subprocesses with proper parameters, and output a clean machine-readable JSON result.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-structured-cli-interoperability/03-RESEARCH.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/pipeline.py
@codes/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Argument Parsing in codes/pipeline.py</name>
  <files>codes/pipeline.py</files>
  <read_first>codes/pipeline.py</read_first>
  <action>Extend `_build_arg_parser()` in `codes/pipeline.py` to support:
  - `--stages`: Comma-separated list of stages to run (default: "planning,analyzing,coding")
  - `--model`: Chat model override
  - `--output-format`: "human" or "json"
  - `--paper-format`: "JSON" or "LaTeX" (default: "JSON")
  - `--pdf-latex-path`: Path to LaTeX source files
  - `--error-file-path`: Path to error message file for debugging
  - `--debug-save-num`: Backups index for debugging (default: 1)
  - `--resume`: Boolean flag to check for and resume incomplete runs (default: False)
  Extend `PipelineConfig` to store all these parameters.
  </action>
  <verify>Run `python codes/pipeline.py --help` to verify all parameters are listed properly.</verify>
  <acceptance_criteria>
    - All arguments are cleanly parsed and mapped to `PipelineConfig` properties.
  </acceptance_criteria>
  <done>CLI arguments are fully parsed and standardized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stdout/stderr Stream Separation</name>
  <files>codes/pipeline.py</files>
  <read_first>codes/pipeline.py</read_first>
  <action>Implement stream redirection in `codes/pipeline.py`:
  - If `args.output_format == "json"`, redirect `sys.stdout` to `sys.stderr` at the start of the `main()` function to prevent diagnostic logs from polluting standard output.
  - At the end of `main()`, restore `sys.stdout` to the original standard output before printing `result.to_json()`.
  </action>
  <verify>Run the script with `--output-format json` and verify that any initial status output is redirected to stderr.</verify>
  <acceptance_criteria>
    - Stream separation prevents any print/log from writing to stdout except the final JSON result.
  </acceptance_criteria>
  <done>stdout/stderr streams are successfully isolated.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Subprocess Stage Orchestration in run_pipeline</name>
  <files>codes/pipeline.py</files>
  <read_first>codes/pipeline.py</read_first>
  <action>Implement `run_pipeline(config)` to execute sub-stages via subprocesses sequentially:
  - Call `init_db(quiet=True)` to initialize database persistence.
  - Call `create_run(...)` using config options to match or create the `run_id`.
  - Execute stage scripts sequentially via `subprocess.run`, mapping config parameters down:
    - If `planning` is active: Run `1_planning.py`, `1.1_extract_config.py`, copy config to repo.
    - If `analyzing` is active: Run `2_analyzing.py`.
    - If `coding` is active: Run `3_coding.py`.
    - If `debugging` is active: Run `4_debugging.py` (ensure error_file_path is provided).
  - Explicitly redirect spawned subprocess outputs: pass `stdout=sys.stderr` and `stderr=sys.stderr` to `subprocess.run` to keep stdout clean.
  - Handle stage failures gracefully: check return codes, log fails, update DB run status using `complete_run(run_id, status="failed" | "completed")`.
  - Fetch aggregated run statistics from the database using `get_run_summary(run_id)` and populate `PipelineResult` with actual tokens, costs, and completed stages.
  </action>
  <verify>Run the pipeline CLI with a small JSON input paper and check if all sub-stages execute in order, write records to SQLite database, and return exit code 0.</verify>
  <acceptance_criteria>
    - Subprocess execution works seamlessly.
    - Child process output is captured and redirected to sys.stderr.
    - The final returned `PipelineResult` is accurate and populated from database metrics.
  </acceptance_criteria>
  <done>Pipeline subprocess stage orchestration is fully implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Add Automated Integration Tests for CLI Interoperability</name>
  <files>tests/test_cli_interop.py</files>
  <action>Create a new integration test suite `tests/test_cli_interop.py` that validates:
  - Invoking `codes/pipeline.py` with `--output-format json` prints only valid JSON to `stdout`.
  - All logs, errors, and informational prints go to `stderr`.
  - Exit code is 0 on success, and non-zero on failure.
  - Sub-stage selection via `--stages` works (e.g. executing only `planning` skips analyzing/coding).
  </action>
  <verify>Run `pytest tests/test_cli_interop.py` and verify all tests pass.</verify>
  <acceptance_criteria>
    - Integration tests cover argument parsing, stream isolation, and stage selection.
  </acceptance_criteria>
  <done>Integration test suite is created and passing.</done>
</task>

</tasks>

<verification>
Execute the following verification steps:
1. Run `python codes/pipeline.py --help` to verify the CLI arguments.
2. Run `pytest tests/test_cli_interop.py` to confirm the stream isolation and stage selection are correctly validated.
</verification>

<success_criteria>
- Spawning the pipeline with `--output-format json` outputs a single, clean JSON block to stdout.
- Progress logs, db status prints, and executor pulling notices write strictly to stderr.
- Stage-based selection via `--stages` and runtime model overrides work perfectly.
</success_criteria>

<output>
Create `.planning/phases/03-structured-cli-interoperability/03-01-SUMMARY.md` after successful execution.
</output>
