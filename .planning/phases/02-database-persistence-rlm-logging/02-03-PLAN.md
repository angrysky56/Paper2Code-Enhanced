---
phase: 02-database-persistence-rlm-logging
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [codes/4_debugging.py]
autonomous: true
requirements: [DB-02]
user_setup: []

must_haves:
  truths:
    - "4_debugging.py parses --run_id argument and initializes DB"
    - "Every execution trial in the sandbox (both initial checks and post-patch checks) logs to the ExecutionTrial table using write_execution_trial"
    - "Every LLM debugging call logs to the StageResult table using write_stage_result"
  artifacts:
    - path: "codes/4_debugging.py"
      provides: "Wired debugging stage and trial persistence"
  key_links: []
---

<objective>
Wire `write_execution_trial()` and `write_stage_result()` logging into the debugging loop in `4_debugging.py`.

Purpose: Log the exact return codes, execution time, stdout/stderr, and success status of every sandbox run in the self-debugging loop, alongside the debugging LLM costs.
Output: An upgraded `codes/4_debugging.py` compiler loop script with comprehensive execution trial tracking.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/4_debugging.py
@codes/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup run_id parser and DB initialization in codes/4_debugging.py</name>
  <files>codes/4_debugging.py</files>
  <read_first>codes/4_debugging.py</read_first>
  <action>Import init_db, write_stage_result, and write_execution_trial in codes/4_debugging.py. Call init_db() after load_dotenv(). Add `--run_id` to parse_args() (type=int, default=-1, can also read from env var RUN_ID).</action>
  <verify>Run codes/4_debugging.py with --help and confirm it compiles and registers --run_id argument.</verify>
  <acceptance_criteria>
    - codes/4_debugging.py parses `--run_id` without error.
  </acceptance_criteria>
  <done>run_id parameter parsing and DB initialization is wired in.</done>
</task>

<task type="auto">
  <name>Task 2: Log initial and verification trials to ExecutionTrial table</name>
  <files>codes/4_debugging.py</files>
  <read_first>codes/4_debugging.py</read_first>
  <action>Insert write_execution_trial calls after BOTH the initial sandboxed check (around line 157) and the post-patch verification check (around line 305). Set correct parameter values (run_id, attempt_num=args.save_num, stdout, stderr, returncode, timed_out, elapsed_seconds, code_dir=debug_dir).</action>
  <verify>Run the script and confirm that execution trials are inserted into the database.</verify>
  <acceptance_criteria>
    - Execution trials are recorded properly for sandbox runs.
  </acceptance_criteria>
  <done>Sandbox execution trials are tracked in the database.</done>
</task>

<task type="auto">
  <name>Task 3: Log debugging LLM calls to StageResult table</name>
  <files>codes/4_debugging.py</files>
  <read_first>codes/4_debugging.py</read_first>
  <action>Insert a write_stage_result call after the LLM api_call completes (around line 280). Use cal_cost to parse actual_input_tokens, output_tokens, cost_usd, and supply messages=msg and output_path.</action>
  <verify>Run the script and confirm StageResult debugging record is created in sqlite.</verify>
  <acceptance_criteria>
    - LLM debugging details are saved as StageResult rows.
  </acceptance_criteria>
  <done>Debugging LLM api calls are tracked in the database.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `codes/4_debugging.py` compiles and executes cleanly.
- [ ] Confirm that running the debugging stage logs trials and stage results into the database.
</verification>

<success_criteria>
- All tasks completed.
- Full trial trajectories (stdout, stderr, exit code, elapsed time, code directory hashes) are logged.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-persistence-rlm-logging/02-03-SUMMARY.md`
</output>
