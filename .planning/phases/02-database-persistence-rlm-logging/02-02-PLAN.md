---
phase: 02-database-persistence-rlm-logging
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified: [codes/1_planning.py, codes/2_analyzing.py, codes/3_coding.py]
autonomous: true
requirements: [DB-02]
user_setup: []

must_haves:
  truths:
    - "Planning, analyzing, and coding stage scripts import and call init_db() at start"
    - "Each stage script captures and logs LLM call metrics via write_stage_result after completion"
  artifacts:
    - path: "codes/1_planning.py"
      provides: "Wired planning stage persistence"
    - path: "codes/2_analyzing.py"
      provides: "Wired analyzing stage persistence"
    - path: "codes/3_coding.py"
      provides: "Wired coding stage persistence"
  key_links: []
---

<objective>
Wire `init_db()` and `write_stage_result()` logging into the planning, analyzing, and coding scripts.

Purpose: Capture the token count, cost, prompt/response messages, and status of every LLM interaction during these stages.
Output: Upgraded stage scripts in `codes/1_planning.py`, `codes/2_analyzing.py`, and `codes/3_coding.py` with structured database auditing.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/1_planning.py
@codes/2_analyzing.py
@codes/3_coding.py
@codes/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable run_id parsing and DB initialization in codes/1_planning.py</name>
  <files>codes/1_planning.py</files>
  <read_first>codes/1_planning.py</read_first>
  <action>Import init_db and write_stage_result in codes/1_planning.py. Call init_db() after load_dotenv(). Parse `--run_id` as a command line argument (type=int, default=-1) or environment variable RUN_ID. Update each of the four LLM calls in the loop to log metrics via write_stage_result using the parsed run_id, using cal_cost to parse tokens and cost, logging input_hash from trajectories, and saving output paths.</action>
  <verify>Run the script with a mock paper/run_id and verify it inserts StageResult records into the database without interrupting pipeline execution.</verify>
  <acceptance_criteria>
    - codes/1_planning.py contains write_stage_result calls after LLM completions.
    - Script accepts `--run_id` arg without errors.
  </acceptance_criteria>
  <done>Planning script persistence logging is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Enable run_id parsing and DB initialization in codes/2_analyzing.py</name>
  <files>codes/2_analyzing.py</files>
  <read_first>codes/2_analyzing.py</read_first>
  <action>Import init_db and write_stage_result in codes/2_analyzing.py. Call init_db() after load_dotenv(). Parse `--run_id` as a command line argument (type=int, default=-1) or environment variable RUN_ID. Update each LLM call in the script to log stage results to the DB.</action>
  <verify>Run the analyzing script with a mock run_id and verify it creates StageResult entries successfully.</verify>
  <acceptance_criteria>
    - codes/2_analyzing.py contains write_stage_result calls.
    - Script accepts `--run_id` arg.
  </acceptance_criteria>
  <done>Analyzing script persistence logging is complete.</done>
</task>

<task type="auto">
  <name>Task 3: Enable run_id parsing and DB initialization in codes/3_coding.py</name>
  <files>codes/3_coding.py</files>
  <read_first>codes/3_coding.py</read_first>
  <action>Import init_db and write_stage_result in codes/3_coding.py. Call init_db() after load_dotenv(). Parse `--run_id` as a command line argument (type=int, default=-1) or environment variable RUN_ID. Update each LLM call in the script to log stage results to the DB.</action>
  <verify>Run the coding script with a mock run_id and verify it creates StageResult entries successfully.</verify>
  <acceptance_criteria>
    - codes/3_coding.py contains write_stage_result calls.
    - Script accepts `--run_id` arg.
  </acceptance_criteria>
  <done>Coding script persistence logging is complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `codes/1_planning.py`, `codes/2_analyzing.py`, and `codes/3_coding.py` compile and execute cleanly.
- [ ] Verify that running these stage scripts inserts proper records into the sqlite database when run_id is supplied.
</verification>

<success_criteria>
- All tasks completed.
- Complete trajectory metadata (input/output tokens, costs, prompts) is logged to SQLModel database for planning, analyzing, and coding.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-persistence-rlm-logging/02-02-SUMMARY.md`
</output>
