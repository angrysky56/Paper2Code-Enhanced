---
phase: 02-database-persistence-rlm-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [codes/db.py]
autonomous: true
requirements: [DB-01]
user_setup: []

must_haves:
  truths:
    - "SQLite database engine is successfully wired using SQLModel"
    - "init_db() creates the database file and all required tables (Run, StageResult, ExecutionTrial) if they do not exist"
  artifacts:
    - path: "codes/db.py"
      provides: "Finalized SQLModel/SQLite initialization logic"
      contains: "create_engine"
  key_links: []
---

<objective>
Finalize the SQLModel/SQLite engine wiring in `codes/db.py` and verify schema initialization.

Purpose: Set up the core database foundation so all downstream pipeline stage runs and trials can be persisted.
Output: Fully operational SQLite database file and schema initialization logic inside `codes/db.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete init_db and session management in codes/db.py</name>
  <files>codes/db.py</files>
  <read_first>codes/db.py</read_first>
  <action>Complete the engine initialization and session management logic in codes/db.py. If sqlmodel is not available, ensure write operations degrade gracefully by returning fallback/dummy values (-1) and logging a warning instead of raising hard errors. Ensure directories are created automatically.</action>
  <verify>Import init_db from codes.db and call it inside a test script to check that the DB engine gets initialized cleanly without exceptions.</verify>
  <acceptance_criteria>
    - calling init_db() creates the db directory and table schema cleanly.
    - calling get_session() works inside a context manager.
  </acceptance_criteria>
  <done>init_db and get_session are robustly implemented and handle ImportErrors gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Implement verification test for DB schema</name>
  <files>tests/test_db_schema.py</files>
  <read_first>codes/db.py</read_first>
  <action>Create a test script at tests/test_db_schema.py or a temporary scratch script that initializes a test SQLite database and verifies that Run, StageResult, and ExecutionTrial tables can be created, and simple records can be inserted and fetched. Check column data types and relationships.</action>
  <verify>Run the schema verification test script and check that all assertions pass.</verify>
  <acceptance_criteria>
    - Verification script inserts a mock Run, StageResult, and ExecutionTrial and fetches them successfully.
  </acceptance_criteria>
  <done>SQLite schema initialization and operations are verified with a test script.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `codes/db.py` compiles without syntax errors.
- [ ] Run `tests/test_db_schema.py` (or equivalent test) and verify successful DB initialization and execution trial storage.
</verification>

<success_criteria>
- All tasks completed.
- SQLite database tables are auto-created cleanly at runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-persistence-rlm-logging/02-01-SUMMARY.md`
</output>
