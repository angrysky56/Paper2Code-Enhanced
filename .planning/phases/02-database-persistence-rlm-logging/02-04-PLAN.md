---
phase: 02-database-persistence-rlm-logging
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified: [codes/db.py, codes/db_query.py]
autonomous: true
requirements: [DB-03, DB-04]
user_setup: []

must_haves:
  truths:
    - "create_run in db.py supports idempotent resume-on-failure by querying active runs with status='running' matching paper_name and output_dir"
    - "A dedicated codes/db_query.py CLI tool is implemented to query sqlite and output beautifully formatted execution analytics"
  artifacts:
    - path: "codes/db_query.py"
      provides: "Formatted database analytics utility"
      contains: "argparse"
  key_links: []
---

<objective>
Implement run-resume query logic in `db.py` and build the `db_query.py` analytics tool.

Purpose: Allow the pipeline to resume interrupted runs idempotently, and provide researchers and operators with a powerful command line tool to query tokens, costs, run durations, and convergence rates.
Output: Upgraded run management logic in `codes/db.py` and a new CLI command utility `codes/db_query.py`.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@codes/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add run-resume idempotency in db.create_run</name>
  <files>codes/db.py</files>
  <read_first>codes/db.py</read_first>
  <action>Modify create_run() in codes/db.py. Before inserting a new Run, query the Run table to check if there is an existing Run with the same paper_name, output_dir, and status in ('running', 'interrupted', 'failed'). If such a run exists, update its status to 'running' (if not already), commit, and return its ID. Add a --resume flag or resume=True optional parameter to create_run() to toggle this behavior if appropriate (default should check/reuse for idempotency).</action>
  <verify>Run db.create_run twice with the same parameters and check that it returns the exact same run_id.</verify>
  <acceptance_criteria>
    - create_run() avoids creating duplicate runs for the same output directory if the previous run did not finish successfully.
  </acceptance_criteria>
  <done>Idempotent run creation and resumption is fully supported.</done>
</task>

<task type="auto">
  <name>Task 2: Develop codes/db_query.py CLI analytics tool</name>
  <files>codes/db_query.py</files>
  <read_first>codes/db.py</read_first>
  <action>Create a new python script at codes/db_query.py. It must use SQLModel/sqlite to provide:
  1. A list/summary of all runs (id, paper_name, model_used, status, duration, total cost).
  2. Detailed query for a specific run_id (tokens_in, tokens_out, stages executed, execution trials list with return codes, elapsed seconds).
  3. Aggregate statistics: total tokens used, total cost USD across all runs, and debugging convergence rates (average number of trials to fix compiler errors).</action>
  <verify>Run python codes/db_query.py and verify that it parses arguments and outputs beautifully formatted CLI reports.</verify>
  <acceptance_criteria>
    - codes/db_query.py exists and supports command line arguments (e.g. --run_id, --summary, --stats).
    - It handles missing databases gracefully.
  </acceptance_criteria>
  <done>CLI DB analytics query tool is fully implemented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify `codes/db_query.py` compiles and executes cleanly.
- [ ] Run verification tests for idempotent run resumption.
</verification>

<success_criteria>
- All tasks completed.
- Runs are resumed idempotently when restarted.
- CLI db_query tool is functional and produces robust aggregate statistics.
</success_criteria>

<output>
After completion, create `.planning/phases/02-database-persistence-rlm-logging/02-04-SUMMARY.md`
</output>
